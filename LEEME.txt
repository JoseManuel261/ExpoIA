INSTRUCCIONES DE INTEGRACIÓN — EXPOIA
========================================

1) Copiar archivos
   Copien el contenido de este paquete DENTRO de su proyecto `expoia` ya
   creado (reemplazando/agregando en `src/`, `public/`, y agregando la
   carpeta `supabase/` y `.env.local.example` en la raíz).

2) Instalar dependencias nuevas
   npm install framer-motion @supabase/supabase-js

3) Variables de entorno
   Copien `.env.local.example` como `.env.local` y llenen:
   - SUPABASE_URL y SUPABASE_SERVICE_ROLE_KEY: Project Settings > API
     dentro de su proyecto de Supabase (org "ExpoIA").
   - CORREO_NOTIFICACIONES y RESEND_API_KEY: pendientes hasta que
     definan el correo oficial y creen la cuenta de Resend.

4) Base de datos
   Entren al SQL Editor de Supabase y ejecuten el contenido de
   `supabase/schema.sql` para crear la tabla `registros_interes`.

5) Correr localmente
   npm run dev
   Abran http://localhost:3000

QUÉ QUEDA FUNCIONANDO YA
-------------------------
- Hero, logos institucionales, Test (5 preguntas, puntaje 100% local)
  y formulario de Registro con validación real.
- El formulario SÍ guarda de verdad en Supabase al enviar (tabla
  registros_interes).

QUÉ QUEDA PENDIENTE (marcado con comentarios TODO en el código)
-----------------------------------------------------------------
- Notificación por correo (Resend): falta el correo oficial de destino
  y verificar un dominio propio para enviar desde él.
- Guardado tipo "Excel": pausado hasta que la universidad confirme los
  requisitos completos del registro oficial (Google Sheets en vivo vs.
  archivo .xlsx descargable). Mientras tanto, la tabla de Supabase
  cumple esa función de respaldo/consulta.
- Tipografía de marca "Aptos"/"Aptos Display": usé un fallback del
  sistema (Segoe UI / system-ui) porque no tengo los archivos de fuente.
  Si consiguen los .woff2 con licencia para uso web, se agregan con
  @font-face en globals.css y quedan aplicadas automáticamente.
ACTUALIZACIÓN v3 — cambios de fondo, no cosméticos
----------------------------------------------------
- Reordené la página: Test y Registro son AHORA lo primero después del
  Hero, literalmente (antes de logos/pasos/sectores/agenda/faq, que
  pasaron a ser contenido de soporte más abajo).
- Cambié el movimiento del holograma: quité el "tilt" con el mouse
  (se sentía mecánico) y lo reemplacé por un campo de luz orgánico
  detrás de la imagen — 3 masas de color cian/magenta con trayectorias
  independientes y mix-blend-mode, que nunca se repiten igual (archivo
  nuevo: HeroField.tsx). Esto es lo que da la sensación de "vivo".
- Tipografía fluida con clamp() en el titular del Hero, en vez de
  saltos bruscos por breakpoint.
- Header con menú móvil real (usa <details> nativo, accesible, sin
  librería extra).
- No hay dependencias nuevas — sigue usando solo framer-motion y
  @supabase/supabase-js, ya instaladas.

Sobre el punto de "todas las páginas hechas contigo se ven iguales":
es un problema real de patrones por defecto (rounded-full, cards con
sombra suave, layout apilado de secciones). Fui explícito rompiendo
esos patrones donde pude en esta vuelta, pero si algo específico les
sigue chirriando, díganme QUÉ puntualmente (un botón, una sección, un
espaciado) y lo corrijo dirigido, en vez de reescribir todo de nuevo
a ciegas.
